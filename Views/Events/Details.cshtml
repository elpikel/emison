@model Emison.ViewModels.Event
@{
    ViewData["Title"] = "Events";
}

<div class="text-center">
    <h1 class="display-4">Details</h1>
    <div>
      <span>@Model.Date</span>
      <span>@Model.Place</span>
    </div>
</div>
